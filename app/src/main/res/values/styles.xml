<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- No Action Bar and Full Screen / No Status Bar Theme -->
    <style name="FullScreenTheme" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Style for the margins of the views -->
    <style name="margins_style">
        <item name="android:layout_marginStart">@dimen/layout_margin_medium</item>
        <item name="android:layout_marginLeft">@dimen/layout_margin_medium</item>
        <item name="android:layout_marginTop">@dimen/layout_margin_medium</item>
        <item name="android:layout_marginEnd">@dimen/layout_margin_medium</item>
        <item name="android:layout_marginRight">@dimen/layout_margin_medium</item>
    </style>

    <!-- Style for the categories which include the article's section, the article's author and the article's date views -->
    <style name="category_style" parent="margins_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/section_text_size</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style for the article title and the article description views -->
    <style name="article_title_style" parent="margins_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
    </style>

</resources>